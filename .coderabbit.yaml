# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json

reviews:
  auto_review:
    base_branches:
      - ".*"
    # Only review PRs carrying this label. Add it to opt a PR in.
    labels:
      - "coderabbit-review"
